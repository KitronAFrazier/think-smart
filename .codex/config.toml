model = "gpt-5.1"

[agents.gameplay]
description = "Owns game loop, grid rules, Troggle behaviors, and UI polish."
config_file = "roles/gameplay.toml"

[agents.backend]
description = "Owns API routes, auth checks, security, and server-side data flow."
config_file = "roles/backend.toml"

[agents.data]
description = "Owns Prisma schema, migrations, performance, and leaderboard queries."
config_file = "roles/data.toml"
